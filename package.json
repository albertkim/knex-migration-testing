{
  "name": "knex-migration-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "knex migrate:latest && knex migrate:currentVersion && node -r dotenv/config server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^2.0.0",
    "knex": "^0.12.6",
    "mysql": "^2.12.0"
  }
}
